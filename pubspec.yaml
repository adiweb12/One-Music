name: one_music_player
description: ONE Ultimate Offline Music Player

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  just_audio: ^0.9.44
  file_picker: ^6.0.0
  flutter_equalizer: ^0.0.5
  flutter_audio_waveforms: ^1.0.6
  flutter_media_metadata: ^0.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
