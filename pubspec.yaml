name: offline_music_player
description: A modern offline music player

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  just_audio: ^0.9.44
  audio_session: ^0.1.7
  file_picker: ^6.0.0
  provider: ^6.0.6
  flutter_equalizer: ^0.0.5
  path_provider: ^2.0.14

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
